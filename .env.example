# Sincronizadas automaticamente pela integração Supabase do Vercel (vercel env pull):
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# Só no servidor (função /api/enrich) — NUNCA prefixar com VITE_:
RAWG_API_KEY=
